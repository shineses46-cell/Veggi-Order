# 작업 기록

## 2026-09-23 — 당일 시세 대기 표시·갱신 시각 조사 자동화 착수

- 당일 데이터가 아직 없는 상태를 휴장과 혼동하지 않도록 그래프 상태를 분리하고, API 갱신 가능 시각을 시간대별로 자동 기록합니다.

## 2026-09-23 — 오늘자 가락시장 시세 확인 수집 착수

- GitHub Actions의 비공개 인증값을 사용해 23일자 API 조회를 실행할 수 있도록 수동 실행 입력값을 보완하고, 결과를 정적 시세 이력에 반영합니다.
- `workflow_dispatch`에서 0(오늘)/1(전날)을 선택할 수 있게 했고, 0으로 수동 실행했습니다.
- 실행 #8이 15초 만에 성공하며 23일자 8개 품목 가격 행을 수집했습니다. 자동 예약 수집의 전날 기준 동작은 유지했습니다.

## 2026-09-23 — 작업 인수인계 문서 정리 착수

- 현재 기능, GitHub Pages 배포, 정적 시세 수집 구조, 알려진 제약과 다음 작업을 민감한 인증값 없이 별도 문서로 정리합니다.
- `HANDOFF.md`를 작성해 다음 대화에서 중단 없이 재개할 수 있는 상태를 기록했습니다.

## 2026-09-23 — 시세 로딩·재고·발주 입력 UX 보완 착수

- 사용자 제보에 따라 시세 새로고침 로딩 표시, 재고 퀵 슬롯 라벨, 발주 수량 저장 흐름, 부족 재고 색상 표시를 점검·개선합니다.
- 새로고침 중 예시 차트를 노출하지 않는 로딩 카드, 숫자 전용 재고 퀵 슬롯, 빨간 부족 상태, 전송 직전 발주 입력 동기화를 구현했습니다.
- `45a3f14 Improve market loading and order input` 커밋을 GitHub Pages에 반영하고 페이지가 `app.js?v=37`을 제공하는 것을 확인했습니다.

## 2026-09-23 — 모바일 시세 탭 호환성 긴급 복구 착수

- 모바일에서 예전 캐시 또는 기존 배포 주소가 남았을 때에도 시세 탭이 멈추지 않도록, 최근에 추가한 관찰자 기반 안내 코드를 제거하는 방식으로 복구합니다.
- 안내 문구 처리를 `MutationObserver` 없이 시세 카드 렌더링 함수로 옮겼습니다. 이로써 모바일/PC의 렌더링 스케줄 차이와 관계없이 반복 관찰이 발생하지 않습니다.
- GitHub Pages에 `e1c5fe1 Remove observer from market rendering`으로 배포하고 새 탭에서 시세 진입 및 실제 카드 렌더링을 확인했습니다.

## 2026-09-23 — 시세 탭 멈춤 긴급 복구 착수

- 시세 탭 진입 불가 제보에 따라 배포된 코드의 실행 오류를 재현하고 원인을 수정합니다.
- `MutationObserver`가 자기 자신의 `innerHTML` 변경을 다시 관찰하는 렌더링 무한 반복이 원인이었습니다.
- `data-market-status-enhanced` 상태로 중복 갱신을 차단하고 GitHub Pages에 `6e5ae74 Fix market tab render loop`로 배포했습니다.
- 실제 새 탭에서 시세 메뉴를 눌러 거래단위·26일 실거래 표기와 8개 카드가 정상 렌더링되는 것을 확인했습니다.

## 2026-09-23 — 시세 거래 없음 안내 문구 정합성 보완 착수

- 비동기 시세 로딩 전에 남던 이전 안내 문구가 실제 그래프의 휴장 처리 방식과 혼동될 수 있어, 실제 데이터가 준비된 뒤 명확한 안내로 교체합니다.
- `market-status-enhancements.js`를 추가해 실제 이력 로딩 후 안내를 `빨간 점·휴 = 거래 없음, 직전 거래일 가격 유지`로 바꿨습니다.
- GitHub 원격 저장소에 커밋 `2d01f0f`로 반영했습니다.

## 2026-09-17 — 퀵 슬롯·시세 그래프 개선 착수

- 재고·발주 공통 퀵 슬롯과 날짜 선택형 시세 그래프 구현을 시작했습니다.
- 재고·발주 퀵 슬롯과 날짜 선택형 시세 그래프를 구현했습니다.

## 2026-09-17 — 전체 현황 입력 개선 착수

- 발주 전체 현황과 품목별 3단계 빠른 재고 입력을 추가하는 작업을 시작했습니다.
- 발주 전체 현황과 3단계 재고 빠른 입력을 구현했습니다.
- 전체 현황 입력 개선 버전을 비공개 주소에 배포했습니다.

## 2026-09-17 — 기능 충돌 정리 착수

- 기존 누적 코드에서 중복 선언과 초기화 순서 문제를 확인했으며, 안정적인 단일 스크립트 구조로 재구성하기 시작했습니다.
- 재고·발주·시세·이력·설정 로직을 하나의 초기화 흐름으로 정리해 초기 렌더링 오류 가능성을 제거했습니다.
- 배포된 화면에서 전체 현황판과 8개 수량 조절 컨트롤이 표시되는 것을 확인했습니다.

## 2026-09-17 — 재고 입력 UX 수정 착수

- 현장 사용 중 제보된 수량 입력 불편을 해결하고, 스크롤 없이 전체 현황을 확인할 수 있도록 재고 화면을 개선하기 시작했습니다.
- 전체 현황판과 증감 버튼을 추가하고, 상세 재고 관리 영역은 필요할 때만 펼치도록 변경했습니다.
- 재고 입력 UX 수정 버전을 비공개 주소에 배포했습니다.

## 2026-09-17 — 재고 관리 고도화 착수

- 빠른 재고 입력, 증감 사유, 폐기 손실, 조사 완료율, 유통기한, 적정 재고, 단위 환산, 권장 발주량, 이상값 경고 기능 구현을 시작했습니다.
- 재고 카드에 빠른 입력과 증감 기록을 추가하고, 당일 조사 진행률·유통기한·재고 상태·환산·권장 발주·이상값을 표시했습니다.
- 재고 입력 알림을 제외한 재고 관리 고도화 버전을 비공개 주소에 배포했습니다.

## 2026-09-17 — 시세 화면 고도화 착수

- 실제 API 연동 전 단계에서 필요한 검색명, 규격·등급, 가격 추이, 경보, 구매가 비교, 대체품목 기능을 준비하기 시작했습니다.
- 예시 시세를 기준으로 7일 추이와 가격 경보를 표시하고, API 승인 뒤 사용할 품목별 검색명·등급·관심 여부·대체 품목 설정을 추가했습니다.
- API 승인 전용 시세 기능 확장 버전을 비공개 주소에 배포했습니다.

## 2026-09-17 — 발주 업무 흐름 고도화 착수

- 실제 거래처 분리 없이도 주문 업무를 처리할 수 있도록, 발주 전송부터 입고 완료까지의 상태 흐름을 추가하는 작업을 시작했습니다.
- 수량·제외·메모·납품일·담당자·마감·예상금액을 발주서에 기록하고, 공유문과 최근 발주 이력에도 반영했습니다.
- 발주 전송과 입고 완료를 분리한 발주 업무 버전을 비공개 주소에 배포했습니다.

## 2026-09-17 — 가락시장 시세 화면 착수

- 가락시장 품목별등급별가격 API 승인 대기 중에도 사용할 수 있도록, 예시 시세와 API 품목명 매핑을 포함한 새 버전 작업을 시작했습니다.
- 시세는 실제값으로 오인되지 않도록 화면 전체에 `API 승인 전 · 예시 시세` 상태를 표시했습니다.
- 시세 탭을 포함한 두 번째 비공개 배포를 완료했습니다.

## 2026-09-14 — 초기 계획

- 식당 야채 재고 및 발주를 위한 순수 HTML/CSS/JavaScript 웹앱 작업을 시작했습니다.

## 2026-09-14 — 핵심 기능 구현

- 모바일 우선 카드형 재고 화면과 하단 메뉴를 구현했습니다.
- 8개 야채 품목의 재고·기준재고·발주량·규격을 반영했습니다.
- localStorage 기반 영속화와 발주 완료 시 기본 입고량 가산 기능을 추가했습니다.

## 2026-09-14 — 점검 완료

- `node --check app.js`로 JavaScript 문법 검사를 통과했습니다.
- 정적 파일만으로 동작하도록 구성했으며 별도의 서버나 의존성이 필요하지 않습니다.

## 2026-09-14 — 기능 확장 계획

- 날짜별 재고·발주 이력, 품목 및 단가 관리, 공유, 엑셀 내보내기와 월별 통계 추가 작업을 시작했습니다.

## 2026-09-14 — 기능 확장 완료

- `localStorage` 데이터 형식을 확장해 품목, 재고, 이력을 함께 보관하고 기존 재고 데이터도 자동 이관하도록 했습니다.
- 달력 이력, 품목 추가/삭제·단가 설정, 기본 공유, 엑셀 호환 내보내기, 월별 사용량 집계를 추가했습니다.
- `node --check app.js` 문법 검사를 통과했습니다.

## 2026-09-14 — 무료 웹 배포 시작

- 모바일 접속을 위한 정적 웹 배포 구성을 시작했습니다.
## 2026-09-17 — 퀵 슬롯 및 날짜별 시세 확인

- 재고와 발주 화면을 한눈에 보는 행 단위 UI로 유지하면서, 각 품목에 빠른 수량 입력 버튼을 추가했습니다.
- 시세 그래프와 7일 날짜 버튼을 연결해 날짜를 누르면 해당 날짜의 정확한 가격이 표시되도록 했습니다.
- 비공개 배포 버전 `appgver_da889ccbc2848191bbe0ddd3b72958f2`를 배포하고 화면 동작을 검증했습니다.

## 2026-09-17 — 그래프·재고 이력 개선

- 시세 그래프에 가격 축, 수평 기준선, 날짜별 클릭 포인트, 선택 날짜 가이드 및 정확한 가격 패널을 추가했습니다.
- 재고 수정을 임시 상태로 보관하고 `재고 최종 확정` 시에만 변화량을 이력으로 기록하도록 변경했습니다.
- 달력에는 입고 기록이 있는 날짜의 입고량을 짧게 표시하도록 했습니다.
- 상세 재고 관리의 입고·사용·폐기·조정, 유통기한, 박스 환산 기능은 유지했습니다.
- 비공개 사이트에 최종 수정본을 배포하고 입력·확정·이력·시세 선택 흐름을 확인했습니다.

## 2026-09-17 — 이력 및 브랜드 표기 보완

- 입고 달력의 날짜 칸에 품목명과 입고량을 표시했습니다.
- 시세 그래프에서 선택 점 위에 가격 말풍선이 나타나도록 추가했습니다.
- 서비스 명칭을 `Veggi Order · 베지 오더`로 변경하고 비공개 배포를 검증했습니다.

## 2026-09-17 — 시세 기간·등급 설정

- 시세 그래프의 날짜 눈금을 데이터 포인트의 중심에 맞췄습니다.
- 최근 7일과 최근 30일 시세를 전환할 수 있게 했고, 30일 화면은 가독성을 위해 5일 간격 눈금을 사용합니다.
- 품목별 조회 등급을 특·상품·중품·하품으로 설정할 수 있게 했습니다. 현재는 예시 가격이며 API 승인 후 실제 등급별 가격으로 전환됩니다.

## 2026-09-17 — API 제외 운영 자동화

- 시세 변동률과 재고 부족 상태를 조합해 발주 추천 문구와 대체 등급 비교 안내를 추가했습니다.
- 발주 전송 후 입고 예정 수량을 재고와 분리 표시했습니다.
- 일일 재고 마감 이력, 유통기한·미입고 알림, JSON 백업·복원을 추가하고 비공개 배포에서 확인했습니다.

## 2026-09-21 — 모바일·PC 공통 UI 안정화

- 모바일과 PC가 서로 다른 누적 렌더링 결과를 보이던 구조를 제거하고, 반응형 단일 화면 구조로 재구성했습니다.
- 시세 7일·30일, 모든 날짜-점 중앙 정렬, 특·상·중 등급, 품목 순서 변경, 재고 보드, 0 기본 발주, 달력 요약·세부 모달, 설정 진입 화면을 포함해 비공개 배포했습니다.

## 2026-09-21 — 가락시장 API 연동 시작

- 승인된 품목별 등급별 가격 JSON API의 정상 응답을 확인했습니다.
- 사용자 표시명과 가락시장 검색명을 분리해 실시간 시세를 연결하는 작업을 시작했습니다.

## 2026-09-21 — 가락시장 API 연동 완료

- 인증값은 배포 서버의 비공개 환경값으로 저장해 브라우저 코드에 포함하지 않았습니다.
- 시세 화면은 API 평균가와 거래단위를 표시하고, 최근 7일·30일 데이터를 기기 캐시와 함께 불러옵니다.
- 거래 데이터가 없는 날짜나 미대응 품목은 기존 예시 시세를 유지하며, 화면에 실시간 적용 일수를 표시합니다.

## 2026-09-21 — 모바일 시세·입력 조작성 보완 시작

- 실사용 중 확인된 시세 차트 선택, 30일 화면 밀도, 등급 반영, 재고·발주 카드 배치 문제를 수정합니다.

## 2026-09-21 — 모바일 시세·입력 조작성 보완 완료

- SVG의 날짜와 점 모두 같은 선택 처리로 연결했고, 선택 가격과 검색일·조회 시각을 카드에 표시했습니다.
- 30일 그래프를 화면 폭에 맞추고 모든 날짜는 작은 글자로 유지해 가로 드래그 없이 확인하게 했습니다.
- 재고·발주 품목을 두 열 카드로 구성하고 발주용 0·기본·2배 퀵슬롯을 추가했습니다.

## 2026-09-21 — 시세 즉시 선택·정보 표기 보완 시작

- 그래프 선택의 반응성과 등급 적용 상태를 분명하게 하고, 시세 데이터의 기준·수집 시각을 개선합니다.

## 2026-09-21 — 시세 즉시 선택·정보 표기 보완 완료

- 그래프 전체를 넓은 터치 영역으로 만들고 선택한 카드만 갱신해 날짜 선택 지연을 줄였습니다.
- API의 `보통` 등급을 앱의 `중` 버튼에 연결하고, 특·상·중 버튼의 즉시 선택 상태를 표시했습니다.
- Y축은 만원 단위로 표시하며, 마지막 수집 시각과 실거래 일수의 뜻을 화면에 명확히 표시했습니다.

## 2026-09-22 — 시세 그래프 레이아웃 복구 시작

- 30일 차트와 카드 정보가 한정된 2열 폭에서 압축된 문제를 단일 열 차트 카드 구조로 수정합니다.

## 2026-09-22 — 시세 그래프 레이아웃 복구 완료

- 시세 목록을 PC에서도 단일 열로 표시해 한 카드에 가격, 등급, 그래프, 안내가 충분한 폭을 갖도록 했습니다.
- 각 시세 카드를 세로 흐름으로 고정하고 차트 폭을 반응형으로 조정해 30일 차트가 옆으로 밀리거나 겹치지 않도록 했습니다.

## 2026-09-22 — 정적 파일 캐시 우회

- 이전 배포 자산이 브라우저에 남아 시세 카드가 과거 레이아웃으로 보이는 문제를 해결하기 위해 CSS와 JavaScript 요청에 버전 값을 추가했습니다.

## 2026-09-22 — 시세 카드 HTML 오류 수정

- 등급 버튼 HTML의 닫는 따옴표 누락으로 카드의 나머지 요소가 첫 번째 등급 버튼에 흡수된 것을 확인하고 수정했습니다.

## 2026-09-22 — 등급 버튼 이벤트 복구

- 위임 방식 클릭 이벤트가 실제 선택값을 바꾸지 않는 것을 재현하고, 버튼별 직접 이벤트 연결로 교체했습니다.

## 2026-09-22 — 등급별 시세 선택 개선

- 선택 등급을 즉시 활성 색상으로 표시하고, 해당 품목 카드만 다시 그려 가격과 그래프를 동기화했습니다.
- 전체 새로고침을 반복하지 않고 등급을 누른 품목의 기간 데이터만 조회하도록 범위를 줄였습니다.

## 2026-09-22 — GitHub 자동 시세 수집 기반 준비

- `.github/workflows/collect-market.yml`: GitHub Actions 수동 실행 및 정기 실행용 워크플로 추가.
- `scripts/collect-market.mjs`: 비밀값을 환경변수로만 읽어 8개 품목의 JSON 시세 스냅샷을 생성.
- `market/latest.json`: 최초 빈 스냅샷 파일 추가.
- 현재 자동화 브라우저의 로컬 파일 업로드 권한 거부로 원격 저장소 반영은 아직 완료되지 않음.

## 2026-09-22 — GitHub 자동 수집 워크플로 반영 및 검증

- 비공개 GitHub 저장소에 `collect-market.yml` 워크플로 커밋 및 첫 실행을 완료했습니다.
- 실행 결과 `market/latest.json`, `market/history.json`이 자동 커밋되는 것까지 확인했습니다.
- `dataJsonOpen`의 XML 응답과 EUC-KR 품목 인코딩을 지원합니다.
- 시세 행이 비어 있는 현재 원인은 코드 오류가 아니라 API 응답 `ERROR_LIMIT_EXCEEDED`(일일 10,000회 초과)입니다.

## 2026-09-22 — 공개 GitHub Pages 배포 완료

- `Veggi-Order` 저장소 공개 전환 및 GitHub Pages 배포 완료.
- `https://shineses46-cell.github.io/Veggi-Order/`에서 재고 화면과 시세 화면을 확인.
- `market-data.js`가 정적 이력 JSON을 `/api/market` 호환 응답으로 변환해 휴대폰의 직접 API 호출과 시세 localStorage 저장을 막습니다.

## 2026-09-23 — 실제 가락시장 시세 수집 복구

- 원인: `dataJsonOpen` 호출은 해당 환경에서 UTF-8 인코딩이어야 실제 JSON 가격 행을 반환함. 안내문의 EUC-KR 방식은 이 JSON 호출에서 빈 응답을 만들었음.
- GitHub Actions 수집기는 UTF-8 쿼리와 전일 거래일 조회 방식으로 수정·배포함.
- 최초 정상 스냅샷을 GitHub `market/latest.json`, `market/history.json`에 반영함. 적채는 데이터 없음 상태로 보존함.
# 2026-09-23 — 시세 비교 기준·기간 전환·연속 드래그 보완 착수

- 특등급 행이 없는 품목은 상등급을 100% 기준으로 삼아 보통 등급의 최고가 비율을 비교하도록 수정한다.
- 7일·30일 전환 직후 더미 차트가 남는 렌더링 순서 문제와 연속 드래그 선택 문제를 함께 해결한다.
- 특등급 데이터가 없으면 상등급 최고가를 비교 기준(100%)으로 자동 선택하게 수정했다.
- `market()`의 렌더 완료 이벤트를 연결해 기간 전환과 로딩 종료 뒤에도 실제 시세 차트를 다시 그린다. 차트 SVG에는 `touch-action: none`과 포인터 캡처를 적용해 드래그 중 날짜 선택이 이어지게 했다.
- 공개 배포본에서 30일 전환 후 실제 거래일 차트와 `보통` 표기를 확인했다.

# 2026-09-23 — 시세 등급 비교·그래프 드래그 선택 착수

- UI의 `중` 표기를 가락시장 API 등급명인 `보통`으로 통일하고, 날짜별 선택 플로팅에 특등급 최고가 대비 상·보통의 비율을 표시하는 작업을 시작했다.
- 그래프에서 좌우 드래그로 선택 날짜를 이동하는 조작을 추가한다.
- 등급 버튼의 내부 값은 기존 호환성을 위해 유지하되 모든 표시 텍스트는 `보통`으로 바꿨다. 플로팅은 선택 날짜의 `특 100% · 상 n% · 보통 n%`를 최고가 기준으로 표시한다.
- 차트 전체에 포인터 드래그 선택을 추가했으며, 그 과정에서 화면을 다시 그려도 SVG 자체는 유지되어 모바일에서도 끊김 없이 날짜를 변경한다.
- GitHub Pages에 `e74d16c Add grade comparison and chart drag selection`으로 배포했고, 공개 배포본의 새 버전 스크립트와 비교·드래그 처리 포함을 확인했다.

# 2026-09-23 — 당일 시세 대기 표시·갱신 시각 조사 자동화

- GitHub Actions 예약 수집을 한국시간 06:17~17:17 매시간으로 임시 확장했습니다. 매 수집 시도는 `market/availability-log.json`에 남겨 API가 실제 시세를 제공하기 시작하는 시간을 관찰합니다.
- 빈 응답은 가격 이력으로 저장하지 않으므로, 아직 갱신되지 않은 당일을 휴장·거래 없음으로 오인하지 않습니다.
- 차트는 기기 기준 오늘 날짜를 자정부터 포함합니다. 오늘 행이 없으면 회색 점선·회색 점·`대기` 라벨을 표시하고, 날짜를 누르면 플로팅에 `시세 수집 대기 / 오늘 경락가 미반영`을 표시합니다.
- `51084a5 Show pending market data and monitor updates` 커밋을 GitHub Pages에 배포했고, 공개 페이지가 `market-data.js?v=3`, `app.js?v=38`, `grade-market.js?v=16`을 제공하는 것을 확인했습니다.

# 2026-09-23 — 최근 30일 시세 초기 적재 시작

- 1일치 실거래 스냅샷으로 인해 빈 그래프 날짜에 같은 가격이 반복 표시되는 현상을 확인했습니다.
- GitHub 정적 시세 이력에 최근 30일의 거래일별 가격을 채워, 휴대폰은 API를 직접 호출하지 않고도 실제 7일·30일 변동을 표시하도록 작업을 시작했습니다.
- 30일(2026-08-24~2026-09-22) 스냅샷을 수집해 `market/history.json`에 반영했습니다. 일요일은 휴장으로 명시됐고, 거래 행이 있는 7개 품목은 각각 26거래일의 실제 가격을 갖습니다.
- 시세 차트는 마지막 수집일을 끝 날짜로 사용하게 바꾸고, 기본 등급을 특으로 통일했습니다.
- GitHub Pages 배포 주소와 GitHub 원본 JSON에서 30일 이력 및 최신 차트 스크립트가 반영됐음을 검증했습니다.

# 2026-09-23 — 그래프 점 선택 안정화

- 등급 전환이 SVG를 다시 쓰면서 넓은 점 선택 영역을 잃던 문제를 수정했습니다.
- 각 점과 그래프 영역 전체에서 날짜 선택이 가능하며, 선택한 실제 평균 경락가 또는 휴장·직전 거래일 상태를 화면에 표시합니다.
- 배포 화면에서 취청오이 9월 16일 점 선택 후 24,278원으로 변경되는 동작을 실제로 확인했습니다.

# 2026-09-23 — 최고가 기준 및 적채 데이터 복구

- 시세 그래프와 대표 금액을 API `MA_P` 최고 경락가 기준으로 전환하고, `AV_P` 평균 경락가를 카드 및 말풍선에 병기했습니다.
- `양배추` API 검색으로 `빨간양배추 국산`을 적채의 정확한 API 검색명으로 확인해, 최근 30일 적채 시세(26 거래일)를 수집했습니다.
- 배포된 화면에서 취청오이의 최고가 22,000원과 평균가 18,404원, 적채의 최고가 17,500원과 평균가 12,604원이 함께 표시되는 것을 검증했습니다.

# 2026-09-23 — 등급·휴장·말풍선 조작성 보완

- 실제 행이 없는 등급은 `없음`으로 표시된 비활성 버튼으로 구분했습니다.
- 휴장일은 회색 점과 `휴` 표기로 보이게 하고, 직전 거래일 가격 유지 사실을 날짜 선택 설명에 표시했습니다.
- 말풍선은 반투명·클릭 통과 처리해 아래 점과 그래프 영역을 선택할 수 있게 했습니다.
- 가락시장 API 응답에는 현재 사용량이나 잔여량을 알려주는 필드·헤더가 없음을 확인했습니다.
- 휴장일 표시를 세로 음영 띠와 `휴` 라벨까지 추가해 30일 그래프에서도 쉽게 알아볼 수 있게 보완했습니다.
- 배포 화면에서 없는 특등급 버튼이 비활성 처리되고, 최고가·평균가 병기가 유지되는 것을 검증했습니다.

# 2026-09-23 — 양상추 단위 검증 및 휴장 점 강조

- 양상추 반복 가격은 API 원본의 6개 거래단위에서 실제로 동일한 최고가가 지속된 결과임을 확인했습니다. 12개 거래단위는 별도로 가격이 변동합니다.
- 거래 없음 점을 짙은 빨간색 채움으로 변경했습니다.

# 2026-09-23 — 거래단위·재고 잔여일·발주 수량 조작

- 품목별 API 거래단위를 시세 카드에서 선택하게 하고 선택값을 기기 저장소에 보관합니다.
- 재고에 기본 입고 주기 기준 잔여일을, 발주에 큰 증감 버튼을 추가했습니다.
- 실제 데이터 로딩 뒤의 시세 상태 문구를 실제 거래일 수로 통일했습니다.
- 휴장일 전일 가격 보정 과정에서 거래 상태가 함께 복사되던 오류를 수정해, 실제 거래일 수와 빨간 휴장 점이 정확히 표시되게 했습니다.
