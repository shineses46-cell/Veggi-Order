# Veggi Order 작업 인수인계

작성일: 2026-09-23 (KST)  
목적: 다음 대화/작업자가 현재 상태를 빠르게 파악하고 안전하게 이어서 작업할 수 있도록 정리한 문서입니다.

> 보안 원칙: 가락시장 API의 `id`, `passwd` 같은 인증값은 이 문서·소스·브라우저에 적지 않습니다. GitHub Actions Repository Secrets에만 보관합니다.

---

## 1. 서비스 개요

**Veggi Order · 베지 오더**는 가족이 식당에서 쓰는 채소 재고·발주·가락시장 시세 확인 웹앱입니다.

- 스마트폰 우선 UI, PC에서도 같은 기능 확인 가능
- 재고·발주·이력·설정은 각 기기 브라우저의 `localStorage`에 저장
- 시세는 휴대폰이 가락시장 API를 직접 호출하지 않음
- GitHub에 저장된 시세 JSON만 읽음 → 휴대폰 저장용량 사용 및 API 호출 낭비를 줄임
- 이용자가 컴퓨터에 익숙하지 않은 어르신도 쓸 수 있도록 큰 버튼·간단한 흐름을 우선함

## 2. 공개 주소와 저장소

| 구분 | 주소 |
|---|---|
| 사용자 앱 (GitHub Pages) | https://shineses46-cell.github.io/Veggi-Order/ |
| GitHub 저장소 | https://github.com/shineses46-cell/Veggi-Order |
| 최근 캐시 우회 확인 주소 | https://shineses46-cell.github.io/Veggi-Order/?v=ux-fix-4d |

### 로컬 작업 경로

| 경로 | 역할 |
|---|---|
| `C:\AI_WORKSPACE\VeggieOrder` | 주 작업 폴더 및 기록 문서 |
| `C:\AI_WORKSPACE\VeggieOrder-GitHubDeploy` | GitHub 원격 저장소와 연결된 배포용 clone |

> 배포할 때는 변경한 파일을 `VeggieOrder-GitHubDeploy`로 복사한 뒤 커밋/푸시했습니다. 원격 자동 수집 커밋과 충돌할 수 있으므로 푸시 전 `git pull --rebase origin main`을 실행합니다.

## 3. 핵심 품목 및 API 검색명

| 앱 표시명 | API 검색명 | 비고 |
|---|---|---|
| 청상추 | 청상추 | 특 등급 없음, 상부터 사용 |
| 취청오이 | 취청오이 | 특·상·중 확인 |
| 수입당근 | 당근 수입 | 특·상·중 확인 |
| 양상추 | 양상추(일반) | 특 없음, `6개`/`12개` 거래단위 선택 |
| 적채 | 빨간양배추 국산 | 특 없음, 상부터 사용 |
| 청양고추 | 청양고추 | 특·상·중 확인 |
| 깻잎 | 깻잎 | 특 없음, `100속`/`4kg상자` 선택 |
| 대파 | 대파(일반) | 특·상·중, `1kg단`/`10kg단` 선택 |

## 4. 가락시장 데이터 구조

### API

- 데이터셋: **(신)유통정보 - 품목별등급별가격(도매시장법인거래)**
- 사용 필드
  - `PUM_NM`: 품목·품명
  - `G_NAME`: 등급
  - `U_NAME`: 거래단위
  - `MI_P`: 최저가
  - `MA_P`: 최고 경락가 → **앱 대표 가격과 그래프 기준**
  - `AV_P`: 평균 경락가 → 카드와 말풍선에 함께 표시
- 가락시장 API는 계정별 일일 호출 한도(확인된 값: 10,000회)를 두지만, 현재 사용량·잔여량을 알려주는 조회 필드나 헤더는 제공하지 않았습니다.

### 정적 수집 방식

1. GitHub Actions가 서버 측에서 API를 호출합니다.
2. 결과를 `market/latest.json`, `market/history.json`에 커밋합니다.
3. 앱의 `market-data.js`가 브라우저 요청(`/api/market`)을 이 정적 JSON으로 변환합니다.
4. 휴대폰은 GitHub Pages JSON만 읽습니다. **앱 시세 조회 자체는 API 한도를 소모하지 않습니다.**

관련 파일:

- `.github/workflows/collect-market.yml`
- `scripts/collect-market-history.mjs`
- `market/history.json`
- `market/latest.json`
- `market-data.js`
- `grade-market.js`

### 보관 이력

- 초기 수집 구간: **2026-08-24 ~ 2026-09-22**, 30일
- 일요일 4일은 거래 없음으로 기록되어, 30일 중 실제 거래일은 보통 26일로 표시됩니다.
- 거래 없는 날은 그래프에 직전 거래일 가격을 이어서 보이되, **짙은 빨간 점 + `휴`** 라벨로 구분합니다.
- 빨간 점이 보이지 않았던 과거 원인: 직전 가격을 복사하면서 `traded: true`까지 같이 복사하던 오류. 현재는 거래 여부를 별도로 보존하도록 수정했습니다.

### 자동 수집 시간

- 현재 GitHub Actions 예약은 06:17 KST입니다 (`cron: 17 21 * * *`).
- API가 실제로 언제 갱신되는지는 아직 충분히 검증하지 않았습니다. 나중에 여러 시간대의 결과를 비교해 최적 시간으로 변경할 예정입니다.
- 수동 실행에서는 `조회 기준일`을 `0`(오늘) 또는 `1`(전날)로 선택할 수 있습니다. 2026-09-23 17:14 KST에 `0`으로 실행해 23일자 8개 품목 행을 성공적으로 받았습니다.

## 5. 현재 구현 기능

### 재고

- 8개 품목을 2열 카드로 한눈에 표시
- `− / 직접 입력 / +` 수량 조절 및 소수 첫째 자리까지만 처리
- 퀵 슬롯: `0 / 기준재고 값 / 기본 입고량 값` (현재 숫자만 표시)
- 기준재고 이하일 때 부족 문구와 재고 현황판 경고 표시
- 부족 문구는 짙은 빨간색
- 재고 수정은 임시 상태로 두고 `재고 최종 확정` 때만 변화량을 이력에 기록
- 기본 입고량·입고 주기를 바탕으로 `약 N일치` 예상 표시
- 발주 전송 뒤에는 입고 예정 수량을 별도 표시
- 일일 재고 마감, 이력, 유통기한 관련 화면 존재

### 발주

- 0보다 큰 수량인 품목만 발주 대상으로 처리
- 숫자 직접입력, 퀵 슬롯, `− / 수량 / +` 조절 지원
- 전송 직전에 입력창 값을 다시 읽어, 입력 직후 전송해도 누락되지 않게 보완
- 발주 내용 복사, 공유용 텍스트, 발주 전송 → 납품 대기 → 입고 완료 흐름
- 입고 완료 시 해당 수량을 재고에 가산하고 이력 기록

### 시세

- 최근 7일/30일 전환
- 대표 가격/그래프: 최고 경락가 `MA_P`
- 평균 경락가 `AV_P`를 함께 표기
- 날짜/그래프 점 선택 시 해당 날짜의 최고·평균가 표시
- 특·상·중 등급 버튼. 실제 행이 없으면 `특 없음` 비활성 버튼
- 여러 거래단위가 있는 품목은 거래단위 선택 버튼
- 시세 카드 순서 위/아래 변경
- 실제 거래일 수 표기 (`최근 30일 중 실제 거래 26일`)
- 새로고침 중에는 이전·예시 그래프를 보여주지 않고 `가락시장 시세를 불러오는 중…` 로딩 카드만 표시

### 이력·설정·백업

- 달력 이력에 발주·입고·재고·마감 표시
- 날짜 클릭 시 세부내역 모달
- 기준재고·기본발주량·단가 설정
- 품목 추가/삭제 기능 기반 존재
- JSON 백업/복원
- 엑셀 호환 내보내기 및 월별 사용량 통계 기반 존재

## 6. 최근 수정과 중요한 회귀 이력

| 커밋 | 내용 |
|---|---|
| `f2dc7e7` | 휴장 가격을 이어 보일 때 거래 상태까지 복사하던 오류 수정 |
| `2d01f0f` | 시세 휴장 안내 문구 추가 |
| `6e5ae74` | 안내 문구 감시기의 무한 렌더링 반복 수정 |
| `e1c5fe1` | `MutationObserver` 기반 안내 코드를 완전히 제거하고 카드 렌더링으로 이동. 모바일 시세 멈춤 방지 |
| `45a3f14` | 시세 로딩 전용 카드, 재고 숫자 퀵 슬롯, 발주 전송 직전 입력 동기화, 부족 문구 빨간색 |

### 반드시 유의할 점

- 모바일에서 시세 탭이 멈춘 적이 있습니다. 원인은 `MutationObserver`가 자기 `innerHTML` 변경을 재감지하는 무한 반복이었습니다.
- 현재 HTML은 `market-status-enhancements.js`를 불러오지 않습니다. 이 스크립트를 다시 활성화하거나 유사한 전역 DOM 감시기를 추가하지 마세요.
- 모바일에 옛 HTML/스크립트 캐시가 남으면 과거 문제가 보일 수 있습니다. 검증 시에는 URL 뒤에 임의의 `?v=...`를 붙여 새 HTML을 받아 확인합니다.

## 7. 현재 소스 주요 파일

| 파일 | 역할 |
|---|---|
| `index.html` | 화면 틀, 스크립트 버전, 소규모 보완 CSS |
| `styles.css` | 반응형 UI 주 스타일 |
| `app.js` | 재고·발주·이력·설정·기본 시세 렌더링과 이벤트 |
| `grade-market.js` | 실제 정적 시세 이력 기반 차트, 등급·거래단위 선택, 휴장 점 |
| `market-data.js` | 정적 JSON을 `/api/market` 형식으로 제공 |
| `inventory-order-enhancements.js` | 재고 잔여일과 발주 `−/+` 보강 |
| `market-status-enhancements.js` | 과거 안내 실험 파일. **현재 미사용** |

## 8. 검증 방법

### 일반 화면

1. 앱 주소를 엽니다.
2. 재고 탭에서 퀵 슬롯이 숫자만 보이는지 확인합니다.
3. 부족 재고를 만들면 `부족 · 기준 ...`이 빨갛게 보이는지 확인합니다.
4. 발주 탭에서 숫자를 입력하고 바로 `발주 전송`을 눌러 전송되는지 확인합니다.
5. 시세 탭에서 등급·거래단위·날짜 선택을 점검합니다.

### 시세 새로고침

1. `시세 새로고침`을 누릅니다.
2. 진행 중에는 그래프 대신 `가락시장 시세를 불러오는 중…` 카드가 보이는지 확인합니다.
3. 완료되면 8개 카드와 실제 최고가·평균가가 보이는지 확인합니다.

> GitHub 정적 JSON이 빠르게 내려오면 로딩 상태가 매우 짧게 지나갈 수 있습니다. 느린 모바일 네트워크에서 더 잘 보입니다.

## 9. 남은 우선 작업

1. **실제 모바일 기기 검증**
   - 시세 탭 멈춤이 완전히 사라졌는지 확인
   - 캐시가 남은 경우 위의 캐시 우회 URL로 먼저 테스트
2. **발주 전송 실사용 확인**
   - 실제 수량을 한 품목만 입력 후 전송/입고완료/재고반영/이력 기록까지 확인
3. **시세 데이터 검증**
   - 품목·등급·거래단위별 최고가와 평균가가 가락시장 원본과 맞는지 표본 대조
   - 양상추 6개/12개처럼 단위별 가격 차이의 사용자 안내 개선 검토
4. **API 수집 시각 실험**
   - API 최신 데이터 갱신 시점 확인 후 GitHub Actions의 06:17 KST 예약 변경 여부 결정
5. **UI 다듬기**
   - 빨간 휴장 점·`휴` 라벨이 모바일에서 충분히 눈에 띄는지 실제 기기로 확인
   - 장기간 사용 시 재고·발주 이력의 정리/백업 동선 검토

## 10. 배포 절차

```powershell
# 작업 폴더에서 문법 점검
node --check app.js
node --check grade-market.js

# 수정 파일을 배포 clone으로 복사한 뒤
git -C C:\AI_WORKSPACE\VeggieOrder-GitHubDeploy add <files>
git -C C:\AI_WORKSPACE\VeggieOrder-GitHubDeploy commit -m "설명"
git -C C:\AI_WORKSPACE\VeggieOrder-GitHubDeploy pull --rebase origin main
git -C C:\AI_WORKSPACE\VeggieOrder-GitHubDeploy push origin main
```

GitHub Pages 전파에는 보통 수십 초가 걸릴 수 있습니다. 배포 확인 시에는 URL에 `?v=검증용문자열`을 붙여 HTML 캐시를 피합니다.

## 11. 사용자 의도와 운영 원칙

- 가족끼리 쓰는 서비스이며, 기술에 익숙하지 않은 어르신도 대상입니다.
- 조작 절차는 짧고 명확해야 합니다.
- 기기별 재고·발주 데이터는 별도로 관리되는 것이 의도된 동작입니다.
- 시세 원본 데이터는 휴대폰에 장기간 저장하지 않고 GitHub 정적 데이터로 중앙 관리합니다.
- API 비밀값은 절대로 웹앱 코드, GitHub Pages, 문서, 화면에 노출하지 않습니다.
